services:
  app:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data        # persiste banco e dados entre runs
      - ./.env:/app/.env
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
